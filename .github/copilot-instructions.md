# GitHub Copilot Instructions: learn-css

- Write modern, standards-compliant CSS utilizing Cascade Layers (`@layer`), CSS Grid, Flexbox, Container Queries, and modern color spaces (`oklch`).
- Ensure high accessibility: honor `prefers-reduced-motion` and `prefers-color-scheme`.
- Write strict TypeScript code with comprehensive Vitest tests.
